data:
  raw_path: "data/raw/ai4i2020.csv"
  processed_path: "data/processed/features.parquet"
  test_size: 0.2
  random_state: 42

model:
  name: "xgboost"
  params:
    n_estimators: 100
    max_depth: 6
    learning_rate: 0.1
    scale_pos_weight: 29
